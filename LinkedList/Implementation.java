import org.w3c.dom.Node;class LinkedList{    Node head;    Node tail;    class Node{        Object data;        Node next;        Node(Object data){            this.data = data;        }    }    public void add(Object data){        Node n = new Node(data);        if(head == null){            head = tail = n;        } else{            tail.next = n;            tail = n;        }    }    public void addFirst(Object data){        Node n = new Node(data);        Node temp = head;        if(head == null){            head = tail = n;        } else{            head = n;            head.next = temp;        }    }    public void addInBetween(Object data, int idx){        Node n = new Node(data);        Node temp = head;        for(int i = 1; i<(idx-1);i++){            temp = temp.next;        }        n.next = temp.next;        temp.next = n;    }     public void display(){        Node temp = head;        while(temp != null){            System.out.print(temp.data + "->");            temp = temp.next;        }         System.out.print("null");              }     public void delete(int idx){        Node temp = head;         for (int i = 1; i < idx-1; i++) {             temp = temp.next;         }         temp.next = temp.next.next;     }    public void deleteLast(){        Node temp = head;        while(temp.next.next != null){            temp = temp.next;        }        tail = temp;        temp.next = null;    }}public class Implementation {    public static void main(String[] args) {        LinkedList l1 = new LinkedList();        l1.add(2);        l1.add(3);        l1.addFirst(1);//        l1.addInBetween(4,2);        l1.deleteLast();        l1.display();    }}